# MarKEN POC Demo — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Transform the existing MarKEN website into a polished, demo-ready POC with light/dark theme, enhanced UI/UX, a free 3D model, and responsive mobile experience — ready for Friday morning presentation.

**Architecture:** Existing Next.js 16 + React 19 + Tailwind v4 + Framer Motion codebase. Add `next-themes` for theme switching. Replace hardcoded dark/light colors with CSS custom properties. Source a free GLTF model from Sketchfab. Add a "Why MarKEN" section to homepage. Polish all existing components with theme-aware styling and improved animations.

**Tech Stack:** Next.js 16.2.1, React 19, Tailwind CSS v4, Framer Motion, next-themes, @react-three/fiber, @react-three/drei, Three.js, lucide-react

**Spec:** `docs/superpowers/specs/2026-03-26-poc-demo-design.md`

**IMPORTANT:** Before writing any Next.js code, read the relevant guide in `node_modules/next/dist/docs/` to confirm API compatibility with Next.js 16.

---

## File Structure

### New Files
- `src/components/ui/ThemeToggle.tsx` — Sun/moon toggle button for header
- `src/components/ui/ThemeProvider.tsx` — next-themes provider wrapper (client component)
- `src/components/home/WhyMarKEN.tsx` — Trust signals / differentiators section

### Modified Files
- `package.json` — Add `next-themes` dependency
- `src/app/globals.css` — Add CSS custom properties for light/dark theme tokens
- `src/app/layout.tsx` — Wrap with ThemeProvider, add `suppressHydrationWarning`
- `src/components/layout/Header.tsx` — Add ThemeToggle, make theme-aware
- `src/components/layout/Footer.tsx` — Make theme-aware
- `src/components/home/HeroSection.tsx` — Redesign for light theme default + theme-aware
- `src/components/home/StatsSection.tsx` — Make theme-aware
- `src/components/home/ProductCategories.tsx` — Make theme-aware with enhanced card hover
- `src/components/home/CTASection.tsx` — Make theme-aware
- `src/app/page.tsx` — Add WhyMarKEN section
- `src/components/product/ProductHero.tsx` — Make theme-aware
- `src/components/product/ProductFeatures.tsx` — Make theme-aware
- `src/components/product/ProductSpecifications.tsx` — Make theme-aware
- `src/components/product/ProductFAQ.tsx` — Make theme-aware
- `src/components/product/ProductDownloads.tsx` — Make theme-aware
- `src/components/3d/Product3DViewer.tsx` — Theme-aware + GLTF model loading
- `src/components/3d/BBR3DExperience.tsx` — Theme-aware wrapper
- `src/components/contact/GetQuoteForm.tsx` — Make theme-aware
- `src/components/ui/WhatsAppFAB.tsx` — Make theme-aware

---

## Task 1: Install next-themes and set up theme infrastructure

**Files:**
- Modify: `package.json`
- Create: `src/components/ui/ThemeProvider.tsx`
- Create: `src/components/ui/ThemeToggle.tsx`
- Modify: `src/app/globals.css`
- Modify: `src/app/layout.tsx`

- [ ] **Step 1: Install next-themes**

```bash
cd /home/pk/WORK/Project/MarKEN_Website && pnpm add next-themes
```

- [ ] **Step 2: Read Next.js 16 docs on client/server components**

```bash
cat node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md | head -100
```

Understand how `"use client"` works in Next.js 16 before creating the provider.

- [ ] **Step 3: Create ThemeProvider client component**

Create `src/components/ui/ThemeProvider.tsx`:

```tsx
"use client";

import { ThemeProvider as NextThemesProvider } from "next-themes";

export default function ThemeProvider({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <NextThemesProvider attribute="class" defaultTheme="light" enableSystem>
      {children}
    </NextThemesProvider>
  );
}
```

- [ ] **Step 4: Create ThemeToggle component**

Create `src/components/ui/ThemeToggle.tsx`:

```tsx
"use client";

import { useTheme } from "next-themes";
import { Sun, Moon } from "lucide-react";
import { useEffect, useState } from "react";

export default function ThemeToggle() {
  const { theme, setTheme } = useTheme();
  const [mounted, setMounted] = useState(false);

  useEffect(() => setMounted(true), []);

  if (!mounted) {
    return <div className="w-9 h-9" />; // Prevent layout shift
  }

  return (
    <button
      onClick={() => setTheme(theme === "dark" ? "light" : "dark")}
      className="p-2 rounded-lg text-slate-500 hover:text-blue-600 dark:text-slate-400 dark:hover:text-blue-400 hover:bg-slate-100 dark:hover:bg-slate-800 transition-colors"
      aria-label={`Switch to ${theme === "dark" ? "light" : "dark"} mode`}
    >
      {theme === "dark" ? (
        <Sun className="w-5 h-5" />
      ) : (
        <Moon className="w-5 h-5" />
      )}
    </button>
  );
}
```

- [ ] **Step 5: Add CSS custom properties for theme tokens to globals.css**

Replace the contents of `src/app/globals.css` with:

```css
@import "tailwindcss";

@theme inline {
  --font-sans: var(--font-inter);
}

/* ---- Theme tokens ---- */
:root {
  --color-surface: #ffffff;
  --color-surface-alt: #f8fafc;
  --color-surface-card: #ffffff;
  --color-text-heading: #0f172a;
  --color-text-body: #334155;
  --color-text-muted: #64748b;
  --color-border: #e2e8f0;
  --color-primary: #2563eb;
  --color-primary-hover: #1d4ed8;
  --color-accent: #0891b2;
  --color-accent-hover: #0e7490;
}

.dark {
  --color-surface: #0f172a;
  --color-surface-alt: #1e293b;
  --color-surface-card: rgba(30, 41, 59, 0.8);
  --color-text-heading: #f8fafc;
  --color-text-body: #cbd5e1;
  --color-text-muted: #94a3b8;
  --color-border: #334155;
  --color-primary: #3b82f6;
  --color-primary-hover: #2563eb;
  --color-accent: #06b6d4;
  --color-accent-hover: #0891b2;
}

body {
  font-family: var(--font-sans), system-ui, -apple-system, sans-serif;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  background-color: var(--color-surface);
  color: var(--color-text-body);
}

html {
  scroll-behavior: smooth;
}

/* Custom scrollbar */
::-webkit-scrollbar {
  width: 8px;
}

::-webkit-scrollbar-track {
  background: var(--color-surface-alt);
}

::-webkit-scrollbar-thumb {
  background: #94a3b8;
  border-radius: 4px;
}

::-webkit-scrollbar-thumb:hover {
  background: #64748b;
}

/* Dark scrollbar */
.dark ::-webkit-scrollbar-track {
  background: #1e293b;
}

.dark ::-webkit-scrollbar-thumb {
  background: #475569;
}
```

- [ ] **Step 6: Wrap layout with ThemeProvider**

Modify `src/app/layout.tsx` — add ThemeProvider wrapper and `suppressHydrationWarning` on `<html>`:

```tsx
import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Header from "@/components/layout/Header";
import Footer from "@/components/layout/Footer";
import WhatsAppFAB from "@/components/ui/WhatsAppFAB";
import OrganizationSchema from "@/components/seo/OrganizationSchema";
import ThemeProvider from "@/components/ui/ThemeProvider";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  // ... keep existing metadata unchanged ...
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} h-full antialiased`} suppressHydrationWarning>
      <head>
        <OrganizationSchema />
      </head>
      <body className="min-h-full flex flex-col font-sans bg-[var(--color-surface)] text-[var(--color-text-body)]">
        <ThemeProvider>
          <Header />
          <main className="flex-1">{children}</main>
          <Footer />
          <WhatsAppFAB />
        </ThemeProvider>
      </body>
    </html>
  );
}
```

- [ ] **Step 7: Verify build passes**

```bash
cd /home/pk/WORK/Project/MarKEN_Website && pnpm build
```

Expected: Build succeeds with no errors.

- [ ] **Step 8: Commit**

```bash
git add src/components/ui/ThemeProvider.tsx src/components/ui/ThemeToggle.tsx src/app/globals.css src/app/layout.tsx package.json pnpm-lock.yaml
git commit -m "feat: add light/dark theme infrastructure with next-themes"
```

---

## Task 2: Make Header theme-aware with ThemeToggle

**Files:**
- Modify: `src/components/layout/Header.tsx`

- [ ] **Step 1: Update Header with theme-aware classes and ThemeToggle**

Key changes:
- Import `ThemeToggle`
- Replace hardcoded white/slate backgrounds with theme-aware classes
- Top bar: `bg-slate-900` stays (always dark brand bar)
- Main nav: `bg-white/95 dark:bg-slate-900/95`
- Logo text: `text-slate-900 dark:text-white`
- Nav links: `text-slate-700 dark:text-slate-300`
- Mega dropdown: `bg-white dark:bg-slate-800`
- Mobile menu: `bg-white dark:bg-slate-900`
- Add `<ThemeToggle />` in the right actions area (before search icon)

The full component must be rewritten with `dark:` variants on every color class. Replace all hardcoded `bg-white`, `text-slate-700`, `text-slate-900`, `border-gray-100` etc. with theme-aware pairs.

- [ ] **Step 2: Verify build passes**

```bash
cd /home/pk/WORK/Project/MarKEN_Website && pnpm build
```

- [ ] **Step 3: Commit**

```bash
git add src/components/layout/Header.tsx
git commit -m "feat: make header theme-aware with dark mode toggle"
```

---

## Task 3: Redesign HeroSection for light-default + theme-aware

**Files:**
- Modify: `src/components/home/HeroSection.tsx`

- [ ] **Step 1: Rewrite HeroSection**

Key design changes:
- Light theme: gradient from blue-50 via white to slate-50. Dark: slate-900 to blue-900 (current design becomes dark variant)
- Background grid and gradient orbs get theme-aware opacity
- Text: `text-slate-900 dark:text-white` for heading, `text-slate-600 dark:text-slate-300` for body
- Badge: light version uses `bg-blue-50 border-blue-200 text-blue-700`, dark keeps current
- CTAs: Primary stays `bg-blue-600 text-white`, secondary gets `border-slate-300 dark:border-white/20`
- Hero visual card: `bg-white dark:bg-white/5 border-slate-200 dark:border-white/10`
- Trust badges: `text-slate-500 dark:text-slate-400`
- Add subtle gradient text on heading for both themes

All `dark:` variants must be explicit on every color class.

- [ ] **Step 2: Verify build**

```bash
cd /home/pk/WORK/Project/MarKEN_Website && pnpm build
```

- [ ] **Step 3: Commit**

```bash
git add src/components/home/HeroSection.tsx
git commit -m "feat: redesign hero section for light theme default with dark mode support"
```

---

## Task 4: Make StatsSection theme-aware

**Files:**
- Modify: `src/components/home/StatsSection.tsx`

- [ ] **Step 1: Update StatsSection with theme classes**

Key changes:
- Section background: `bg-slate-50 dark:bg-slate-900` (light uses subtle gray, not white, for contrast)
- Counter text: `text-slate-900 dark:text-white`
- Suffix: `text-blue-600 dark:text-blue-400`
- Label: `text-slate-700 dark:text-slate-300`
- Description: `text-slate-500 dark:text-slate-500`
- Heading: `text-slate-900 dark:text-white`
- Subtext: `text-slate-500 dark:text-slate-400`

- [ ] **Step 2: Verify build**

```bash
cd /home/pk/WORK/Project/MarKEN_Website && pnpm build
```

- [ ] **Step 3: Commit**

```bash
git add src/components/home/StatsSection.tsx
git commit -m "feat: make stats section theme-aware"
```

---

## Task 5: Make ProductCategories theme-aware with enhanced cards

**Files:**
- Modify: `src/components/home/ProductCategories.tsx`

- [ ] **Step 1: Update ProductCategories**

Key changes:
- Section: `bg-white dark:bg-slate-950`
- Section heading label: `text-blue-600 dark:text-blue-400`
- Section heading: `text-slate-900 dark:text-white`
- Section subtext: `text-slate-500 dark:text-slate-400`
- Cards: `bg-white dark:bg-slate-800/50 border-slate-200 dark:border-slate-700`
- Card hover: `hover:border-blue-300 dark:hover:border-blue-500 hover:shadow-lg dark:hover:shadow-blue-500/10`
- Card title: `text-slate-900 dark:text-white group-hover:text-blue-600`
- Card description: `text-slate-500 dark:text-slate-400`
- Image placeholder gradient: `from-slate-200 to-slate-300 dark:from-slate-700 dark:to-slate-600`

- [ ] **Step 2: Verify build**

```bash
cd /home/pk/WORK/Project/MarKEN_Website && pnpm build
```

- [ ] **Step 3: Commit**

```bash
git add src/components/home/ProductCategories.tsx
git commit -m "feat: make product categories theme-aware with enhanced card hover"
```

---

## Task 6: Create WhyMarKEN section + add to homepage

**Files:**
- Create: `src/components/home/WhyMarKEN.tsx`
- Modify: `src/app/page.tsx`

- [ ] **Step 1: Create WhyMarKEN component**

Create `src/components/home/WhyMarKEN.tsx`:

A trust signals section with 4 differentiator cards:
1. "25+ Years of Innovation" — Shield icon, history/trust message
2. "WHO PQS Prequalified" — Award icon, international standards
3. "Pan-India Service Network" — MapPin icon, 500+ service centers, 30 franchises
4. "Complete Healthcare Solutions" — Building icon, 400+ products across 6 categories

Design:
- Section: `bg-white dark:bg-slate-900`
- Cards: icon in blue circle, title, description. 4-column grid on desktop, 2 on tablet, 1 on mobile
- Framer Motion fade-up with stagger
- All theme-aware

- [ ] **Step 2: Add WhyMarKEN to homepage**

Modify `src/app/page.tsx` — insert WhyMarKEN between StatsSection and CTASection:

```tsx
import HeroSection from "@/components/home/HeroSection";
import ProductCategories from "@/components/home/ProductCategories";
import StatsSection from "@/components/home/StatsSection";
import WhyMarKEN from "@/components/home/WhyMarKEN";
import CTASection from "@/components/home/CTASection";

export default function HomePage() {
  return (
    <>
      <HeroSection />
      <ProductCategories />
      <StatsSection />
      <WhyMarKEN />
      <CTASection />
    </>
  );
}
```

- [ ] **Step 3: Verify build**

```bash
cd /home/pk/WORK/Project/MarKEN_Website && pnpm build
```

- [ ] **Step 4: Commit**

```bash
git add src/components/home/WhyMarKEN.tsx src/app/page.tsx
git commit -m "feat: add why marken trust signals section to homepage"
```

---

## Task 7: Make CTASection theme-aware

**Files:**
- Modify: `src/components/home/CTASection.tsx`

- [ ] **Step 1: Update CTASection**

The CTA uses a blue gradient background — this works for both themes. Only adjust:
- Ensure text contrast works on both themes (it does — white on blue is fine)
- The section needs no major changes since it has its own background
- Optional: add a subtle border top `border-t border-slate-200 dark:border-slate-800` if needed for visual separation

- [ ] **Step 2: Verify build**

```bash
cd /home/pk/WORK/Project/MarKEN_Website && pnpm build
```

- [ ] **Step 3: Commit**

```bash
git add src/components/home/CTASection.tsx
git commit -m "feat: make CTA section theme-aware"
```

---

## Task 8: Make Footer theme-aware

**Files:**
- Modify: `src/components/layout/Footer.tsx`

- [ ] **Step 1: Update Footer**

The Footer currently uses `bg-slate-900` which works well for both themes as a consistent dark footer. Keep it as-is for dark/brand consistency. Only change:
- No theme changes needed — dark footer is a common pattern even in light-themed sites
- Keep the footer as-is

This task may be a no-op. Verify it looks good in both themes during visual review.

- [ ] **Step 2: Verify build**

```bash
cd /home/pk/WORK/Project/MarKEN_Website && pnpm build
```

- [ ] **Step 3: Commit (only if changes made)**

```bash
git add src/components/layout/Footer.tsx
git commit -m "feat: verify footer works in both themes"
```

---

## Task 9: Make product page components theme-aware

**Files:**
- Modify: `src/components/product/ProductHero.tsx`
- Modify: `src/components/product/ProductFeatures.tsx`
- Modify: `src/components/product/ProductSpecifications.tsx`
- Modify: `src/components/product/ProductFAQ.tsx`
- Modify: `src/components/product/ProductDownloads.tsx`

- [ ] **Step 1: Read all product components to understand current styling**

Read each file to see current hardcoded colors.

- [ ] **Step 2: Update ProductHero**

Add `dark:` variants to all color classes. Key: section background, text colors, badge colors, CTA button.

- [ ] **Step 3: Update ProductFeatures**

Add `dark:` variants. Key: card backgrounds, icon colors, text, borders.

- [ ] **Step 4: Update ProductSpecifications**

Add `dark:` variants. Key: table backgrounds, alternating rows, text, variant selector.

- [ ] **Step 5: Update ProductFAQ**

Add `dark:` variants. Key: accordion background, border, text, expanded state.

- [ ] **Step 6: Update ProductDownloads**

Add `dark:` variants. Key: card backgrounds, icon colors, download button.

- [ ] **Step 7: Verify build**

```bash
cd /home/pk/WORK/Project/MarKEN_Website && pnpm build
```

- [ ] **Step 8: Commit**

```bash
git add src/components/product/
git commit -m "feat: make all product page components theme-aware"
```

---

## Task 10: Make 3D experience pages theme-aware

**Files:**
- Modify: `src/components/3d/Product3DViewer.tsx`
- Modify: `src/components/3d/BBR3DExperience.tsx`

- [ ] **Step 1: Update Product3DViewer**

Key changes:
- Container: `bg-gradient-to-b from-slate-50 to-white dark:from-slate-900 dark:to-slate-950`
- Border: `border-slate-200 dark:border-slate-700`
- Top bar label: `text-slate-800 dark:text-white bg-white/80 dark:bg-slate-800/80`
- Buttons: `bg-white/80 dark:bg-slate-800/80 text-slate-600 dark:text-slate-300`
- Info panel: `bg-white/95 dark:bg-slate-800/95 border-slate-200 dark:border-slate-700`
- Info panel text: `text-slate-900 dark:text-white` for title, `text-slate-600 dark:text-slate-300` for body
- Loading fallback: theme-aware background

- [ ] **Step 2: Update BBR3DExperience**

Read the file first, then add `dark:` variants as needed.

- [ ] **Step 3: Verify build**

```bash
cd /home/pk/WORK/Project/MarKEN_Website && pnpm build
```

- [ ] **Step 4: Commit**

```bash
git add src/components/3d/
git commit -m "feat: make 3D viewer and experience pages theme-aware"
```

---

## Task 11: Make GetQuoteForm and WhatsAppFAB theme-aware

**Files:**
- Modify: `src/components/contact/GetQuoteForm.tsx`
- Modify: `src/components/ui/WhatsAppFAB.tsx`

- [ ] **Step 1: Read GetQuoteForm to understand current styling**

```bash
cat src/components/contact/GetQuoteForm.tsx
```

- [ ] **Step 2: Update GetQuoteForm**

Add `dark:` variants to: form background, input fields, labels, borders, submit button, sidebar cards.

- [ ] **Step 3: Update WhatsAppFAB**

The FAB uses green/blue/slate colors that work on both themes. Minor tweaks:
- Collapsed button bg stays green
- Expanded close button: `bg-slate-800 dark:bg-slate-700`

- [ ] **Step 4: Verify build**

```bash
cd /home/pk/WORK/Project/MarKEN_Website && pnpm build
```

- [ ] **Step 5: Commit**

```bash
git add src/components/contact/GetQuoteForm.tsx src/components/ui/WhatsAppFAB.tsx
git commit -m "feat: make quote form and WhatsApp FAB theme-aware"
```

---

## Task 12: Source and integrate free 3D model

**Files:**
- Add: `public/models/refrigerator.glb` (downloaded free model)
- Modify: `src/components/3d/Product3DViewer.tsx` (update model loading)
- Modify: `src/components/3d/BBR3DExperience.tsx` (pass model URL)
- Modify: `src/data/products.ts` (add modelUrl field)

- [ ] **Step 1: Search Sketchfab for free CC-licensed refrigerator/medical equipment GLTF**

Search for free downloadable GLTF/GLB models. Acceptable search terms:
- "refrigerator" OR "medical refrigerator" OR "laboratory refrigerator" OR "commercial refrigerator"
- Filter: downloadable, free, CC license
- Prefer models with: glass door, metallic body, clean geometry

If no medical refrigerator found, a clean commercial refrigerator or lab equipment model works.

Download the GLB file to `public/models/refrigerator.glb`.

- [ ] **Step 2: Test model loads**

Check the file size and validity:

```bash
ls -la public/models/refrigerator.glb
file public/models/refrigerator.glb
```

If the model is larger than 10MB, consider using `gltf-transform` to optimize:

```bash
npx @gltf-transform/cli optimize public/models/refrigerator.glb public/models/refrigerator.glb
```

- [ ] **Step 3: Update Product3DViewer to use the model**

The GLTFModel component already exists. Update the `RefrigeratorModel` fallback and adjust the GLTF model scale/position as needed after testing visually.

Hotspot positions may need adjustment depending on the model geometry — calibrate after visual testing.

- [ ] **Step 4: Update BBR3DExperience to pass modelUrl**

Pass `modelUrl="/models/refrigerator.glb"` from the experience page.

- [ ] **Step 5: Verify build and test in dev**

```bash
cd /home/pk/WORK/Project/MarKEN_Website && pnpm build
```

Then test in dev:
```bash
pnpm dev
```

Navigate to `/3d-experience/blood-bank-refrigerator` and verify model loads.

- [ ] **Step 6: Commit**

```bash
git add public/models/ src/components/3d/ src/data/products.ts
git commit -m "feat: add free 3D model for blood bank refrigerator viewer"
```

---

## Task 13: Final visual polish and responsive check

**Files:**
- Various components may need minor tweaks

- [ ] **Step 1: Run dev server and visual review all pages**

```bash
cd /home/pk/WORK/Project/MarKEN_Website && pnpm dev
```

Check each page in both light and dark themes:
1. Homepage — all sections render correctly in both themes
2. Product page — all sections theme-aware
3. 3D Experience — model loads, hotspots work, theme-aware chrome
4. Get Quote — form renders in both themes
5. Toggle theme on each page — smooth transition, no flash

- [ ] **Step 2: Check responsive breakpoints**

Test at:
- Mobile: 375px width
- Tablet: 768px width
- Desktop: 1280px width

Key checks:
- Hamburger menu works on mobile
- 3D viewer has touch controls
- Cards stack properly
- Text remains readable
- No horizontal overflow

- [ ] **Step 3: Fix any issues found during visual review**

Apply targeted fixes. Each fix is a separate small edit.

- [ ] **Step 4: Final build verification**

```bash
cd /home/pk/WORK/Project/MarKEN_Website && pnpm build
```

Expected: Build succeeds, all 5 routes generate as static pages.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "fix: visual polish and responsive fixes for POC demo"
```

---

## Task 14: Update project memory

**Files:**
- Modify: `/home/pk/.claude/projects/-home-pk-WORK-Project-MarKEN-Website/memory/project_build_status.md`

- [ ] **Step 1: Update build status memory**

Update the memory file to reflect current state: theme system added, 3D model integrated, all components theme-aware, POC demo-ready.

- [ ] **Step 2: Commit (no commit needed for memory files)**

Memory files are outside the project repo.

---

## Execution Summary

| Task | Description | Est. Time |
|------|-------------|-----------|
| 1 | Theme infrastructure (next-themes, CSS vars, provider) | 5 min |
| 2 | Header theme-aware + toggle | 5 min |
| 3 | HeroSection redesign for light default | 5 min |
| 4 | StatsSection theme-aware | 3 min |
| 5 | ProductCategories theme-aware | 3 min |
| 6 | WhyMarKEN section + homepage | 5 min |
| 7 | CTASection theme-aware | 2 min |
| 8 | Footer check | 2 min |
| 9 | Product page components (5 files) | 10 min |
| 10 | 3D viewer theme-aware | 5 min |
| 11 | GetQuoteForm + WhatsAppFAB | 5 min |
| 12 | 3D model sourcing + integration | 15 min |
| 13 | Visual polish + responsive check | 10 min |
| 14 | Update project memory | 2 min |

**Total: ~77 minutes of execution time**

Dependencies: Tasks 1 must complete before all others. Tasks 2-11 can be parallelized. Task 12 is independent. Task 13 must be last.
